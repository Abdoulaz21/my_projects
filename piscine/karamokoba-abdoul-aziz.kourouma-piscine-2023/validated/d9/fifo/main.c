#include <stdio.h>
#include <stdlib.h>

#include "fifo.h"

int main(void)
{
    /*struct fifo *fifo1 = fifo_init();
    **struct fifo *fifo2 = fifo_init();
    **
    **printf("size of fifo1 : %zu\n",fifo_size(fifo1));
    **printf("size of fifo2 : %zu\n",fifo_size(fifo2));
    **
    **fifo_print(fifo1);
    **fifo_push(fifo1,2020);
    **puts("2020 added");
    **fifo_print(fifo1);
    **fifo_push(fifo1,42);
    **puts("42 added");
    **fifo_print(fifo1);
    **fifo_push(fifo1,41);
    **puts("41 added");
    **fifo_print(fifo1);
    **fifo_push(fifo1,2019);
    **puts("2019 added");
    **fifo_print(fifo1);
    **
    **printf("size of fifo1 : %zu\n",fifo_size(fifo1));
    **printf("head of fifo1 : %d\n",fifo_head(fifo1));
    **fifo_print(fifo1);
    **
    **fifo_pop(fifo1);
    **
    **printf("head of fifo1 after pop: %d\n",fifo_head(fifo1));
    **fifo_print(fifo1);
    **
    **fifo_clear(fifo1);
    **printf("size of fifo1 after clear: %zu\n",fifo_size(fifo1));
    **fifo_print(fifo1);
    **fifo_print(fifo2);
    **
    **fifo_push(fifo1,42);
    **fifo_print(fifo1);
    **fifo_destroy(fifo1);
    */
    return 0;
}
